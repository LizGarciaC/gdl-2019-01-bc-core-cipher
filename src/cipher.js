window.cipher = {
  let x;
  let n=0;
  let i=0;
  let codeMensaje;


let code=()=>{
  x=document.getElementById('boxSting').value;
  n=document.getElementById('boxOffset').value;

  for (i=0; i<=x.lengt, i++);
  codeMensaje= ((x.charCodeAt(i))-65+n)%26+65
  prompt (codeMensaje);

}





  // ... 
};
